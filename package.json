{
  "name": "currencyconv",
  "version": "1.0.0",
  "description": "Command line program that allows you to do currency conversions.",
  "main": "dist/app.js",
  "bin": {
    "currencyconv": "dist/app.js"
  },
  "directories": {
    "bin": "./bin"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=dev concurrently \"tsc -w\" \"nodemon -q .\"",
    "start": "cross-env NODE_ENV=prod node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yann LE COZ",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.19",
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "concurrently": "^7.2.2",
    "cross-env": "^7.0.3"
  }
}
